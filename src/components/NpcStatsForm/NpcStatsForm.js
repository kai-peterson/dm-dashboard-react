import React from 'react';
import './NpcStatsForm.css';

const NpcStatsForm = () => {
    return (
        <div className="npc-stats-form-container">
            Two grid columns: one with stats and input fields and a second with two radio buttons to denote if proficient. Bonus to skills should be automatically calculated when stats are input, but still changeable in-case character is a rogue for example.
        </div>
    )
}

export default NpcStatsForm
